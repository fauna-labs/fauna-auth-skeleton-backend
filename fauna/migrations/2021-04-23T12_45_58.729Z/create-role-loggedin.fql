CreateRole({
  name: "loggedin",
  membership: [{
    resource: Collection("accounts"),
    predicate: Query(Lambda("ref", And(HasCurrentToken(), Equals(Select(["data", "type"], Get(CurrentToken()), false), "access"))))
  }],
  privileges: [{
    resource: Collection("dinos"),
    actions: {
      read: true
    }
  }]
})